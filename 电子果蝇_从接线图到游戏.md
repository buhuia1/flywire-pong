# 我们把果蝇的什么放进了电脑？

## 一句话

当前程序使用真实果蝇的神经元连接图，给每个节点配上简化的活动方程，再通过人为设计的游戏输入和事先训练的动作读出来控制球拍。它没有复制一只活果蝇的完整大脑、身体、记忆或学习过程。

## 1. 什么来自真实数据，什么来自我们的程序？

| 层次 | 当前采用的方法 | 与真实果蝇的关系 |
|---|---|---|
| 节点身份 | 138,639 个真实神经元 ID | 来自 Shiu 项目整理的 FlyWire v783 神经元集合 |
| 谁连谁 | 15,091,983 条有向神经元对连接 | 来自电镜重建；多处突触汇总为一条连接，约对应 5,449 万个突触接触 |
| 连接强度 | 按突触数量构造权重，再按每个接收细胞的总输入归一化 | 数量来自解剖；不是逐突触实测电流 |
| 正负号 | 沿用上游模型根据递质预测采用的正负号 | 是带假设的功能解释，不是所有突触逐一实测的兴奋/抑制效应 |
| 每个细胞怎样活动 | 一个有记忆、会衰减、会饱和的数值 | 人为选择的 tanh 速率模型，非原论文 LIF 放电模型 |
| 怎样看到球 | 直接取得坐标和速度，混合后输入 314 个 LC4/LPLC2 节点 | 输入节点有真实身份；编码不是实测视觉反应，也没有经过完整视网膜通路 |
| 怎样移动球拍 | 从 192 个下游节点读数，再用已训练的线性读出器生成动作 | 接口由我们设计；这些细胞不是已证实的“果蝇乒乓运动神经元” |
| 怎样学习 | 事先用“跟球规则”的示范训练读出器 | 只训练外部读出，游戏中参数冻结，连接图也冻结 |

可以把连接组想成道路图：知道道路连接了哪些地点，可以研究信息可能怎样走；但道路图本身并不包含当时的车流、司机策略和交通信号变化。

## 2. 规则跟球到底是什么？

球在球拍上方就向上移动，在下方就向下移动；偏差越大，移动指令越强，直到达到速度上限。

代码的核心是 `action = clip(9 × (球的高度 − 左球拍高度), −1, 1)`。屏幕坐标向下增加，所以负动作向上、正动作向下。

这个对照不经过果蝇网络，也不需要训练。同一个规则还提供了我们训练动作读出器时的示范答案。果蝇组的动作读出器只能读取神经活动；直接状态组读取游戏坐标；两者都在学习逼近这个示范。

因此，这个简单任务上，果蝇图、随机重连图和直接状态模型成绩接近并不奇怪。当前结果未证明果蝇拓扑有独特优势，也未证明需要整个大脑才能完成任务。

## 3. 真实果蝇怎样成为电脑中的模型？

研究者先将脑组织固定、切片，用电子显微镜成像；再由算法与人工校对追踪细胞、识别突触，得到神经元身份、形态和连接表。电脑把连接表存成矩阵，并为神经元和突触添加数学规则，才能计算随时间变化的活动。电镜给出结构，活动变化需要模型和额外实验提供约束。

Shiu 等人的原模型采用 LIF：细胞积累输入，电位达到阈值后发放一次脉冲，再重置。我们这个 Pong 原型使用更简化的连续数值更新，并重新设计了游戏输入与动作读出。不能把原论文的生物学验证成绩直接算在当前 Pong 程序上。

来源：[FlyWire 连接组论文](https://www.nature.com/articles/s41586-024-07558-y)、[Shiu 等计算模型论文](https://www.nature.com/articles/s41586-024-07763-9)。

## 4. 电脑够不够？

这台电脑的 RTX 4060 Ti 8 GB、约 32 GB 内存足以运行当前点神经元模型。接入脑图后的一次本机检查约为每秒 20 次游戏更新，每次网络计算约 2.4 ms；这个数值不是严格的硬件性能基准，多人同时在线或其他程序负载会影响速度。

大的节点数量不等于每个节点都做复杂生理计算。一个 float32 活动值只占 4 字节，138,639 个值约 0.53 MiB；主要成本在稀疏连接表及其计算。我们没有把每个细胞的树突分段、离子通道、分子反应和完整身体都建进模型。

完整电镜图像体积与当前连接表不是一个量级，官方建议通过云端按需读取；不能用当前游戏能实时运行，推断本机可以载入和计算全部高精度生物细节。[官方数据访问说明](https://github.com/seung-lab/FlyConnectome)

## 5. 三维图现在显示什么？

本地地址：<http://127.0.0.1:8741/brain>。朋友使用原分享地址后加 `brain`，也可从游戏右侧的三维图链接打开。游戏与脑图应使用同一个浏览器，才能对应同一游戏会话。

官方标注与模型匹配后，有 118,086 个节点带胞体坐标。网页显示其中固定抽样的 12,583 个，包含全部 314 个输入胞体，以及有坐标的 191 个真实图读出胞体；计算仍覆盖全部模型节点。剩余 20,553 个节点没有胞体坐标，不能编造位置来补齐。

先看“官方细胞大类”了解空间分布；再到游戏页发球或开启自动演示，切换脑图的“模拟活动强度”。拖动旋转，滚轮缩放，点击点可查看 ID；暂停游戏可保留当前网络状态。

活动颜色为 `|x|`，固定对数色标从 10⁻⁶ 到 1，单位 a.u.；绝对值不区分状态正负，不能用该颜色推断细胞是兴奋性还是抑制性。细胞类型排名计算同类所有模型节点的平均绝对状态，只列同类至少 5 个的已标注类型。它不是脑区激活统计，亦未扣除刺激前基线。输入细胞由程序直接驱动，较亮不能证明它对接球有特殊因果作用。

源码：[官方标注仓库](https://github.com/flyconnectome/flywire_annotations)，下载 commit、文件哈希与坐标变换记录在 `data/anatomy_manifest.json`。当前位置图没有树突/轴突骨架、突触位置或脑区网格。

## 6. 和真实果蝇的差距在哪里？

不能给一个“还原了百分之几”的可信总分。不同层次要分别验证：

- **解剖层面**：连接组有实测依据，但有重建误差、缺失细胞与突触；单只固定个体不代表所有果蝇。
- **生理层面**：当前每个细胞只有一个数值，没有真实膜电位、脉冲时序、细胞内空间结构与细胞特异生理参数。
- **突触与内部状态**：递质受体、神经调质、饥饿、疲劳与慢变化等没有按生理方式建模；突触数量不能完整决定传递效力。
- **身体与感知**：当前直接读取数字坐标，没有复眼成像、完整视觉处理、腿和翅膀、身体感觉反馈。
- **学习与经验**：没有真实果蝇的发育史与记忆，也没有在线突触可塑性；读出器的监督训练属于工程学习。
- **实验验证**：当前通过的是软件一致性和 Pong 对照测试，没有以真实果蝇活动或行为验证我们的 Pong 模型。

原论文也明确讨论形态、受体动态、神经调质与基线活动等简化的限制。原文报告的部分实验预测一致率不是“整只果蝇还原程度”。[模型论文及其局限](https://www.nature.com/articles/s41586-024-07763-9)

## 7. 怎样把它推进成研究？

游戏原型适合验证数据到闭环控制的技术链。要研究真实果蝇机制，建议先复现已有生物验证的感觉—动作回路：固定输入细胞与输出细胞，比较实测图和打乱图，预测某类细胞的激活或沉默怎样改变下游响应，再与真实记录和行为比较。

“哪个脑区在起作用”应从可靠的细胞类型和突触脑区标注出发，先做刺激前后响应对照，再在模型里干预候选细胞；最后用独立活体实验检验。模型用于提出和筛选可检验的假设，湿实验决定这些假设是否适用于真实果蝇。
